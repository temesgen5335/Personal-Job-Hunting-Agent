{
  "mcpServers": {
    "personalagent": {
      "type": "stdio",
      "command": "${CLAUDE_PROJECT_DIR:-.}/.venv/bin/python",
      "args": ["-m", "jobagent.mcp"],
      "env": {
        "JOBAGENT_DB_PATH": "${JOBAGENT_DB_PATH:-data/jobagent.db}"
      }
    }
  }
}
